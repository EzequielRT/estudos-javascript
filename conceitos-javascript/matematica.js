export const criador = 'Ezequiel';

export let somar = (x, y) => {return x+y};

export let subtrair = (x, y) => {return x-y};

export let dividir = (x, y) => {return x/y};

export let multiplicar = (x, y) => {return x*y};